'use strict';

const namen = {
  // key: value,
  // property: value,
  vorname: "Anja",
  nachname: "Dannenberg",
}

let arrayVonName = Object.values(namen);
let arrayVonEintraegen = Object.entries(namen);
console.log('arrayVonEintraegen --->', arrayVonEintraegen);